version: 2
jobs:
  build:
    docker:
      - image: gcc:latest
    steps:
      - checkout
      - run:
          name: build
          command: make
      - run:
          name: test
          command: ./test
